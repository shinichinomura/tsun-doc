import React from 'react'

import AddedDate from '../components/AddedDate.jsx'

const Root = (params) => (
  <div className="user_amazon_book_added_date">
    <AddedDate {...params} />
  </div>
)

export default Root
